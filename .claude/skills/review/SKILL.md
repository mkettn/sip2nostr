---
name: review
description: >
  Review a sip2nostr PR, branch, or working diff. Use when asked to review
  changes, check a PR, assess a commit, or judge whether a follow-up commit
  addressed earlier review feedback.
---

# Reviewing sip2nostr changes

Read `agents/reviewing.md` and follow it.

The rules live there rather than here so tools that don't read
`.claude/skills/` can still find them. This file exists only so Claude Code
loads them on demand instead of in every session.
