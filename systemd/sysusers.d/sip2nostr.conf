# Creates the sip2nostr system user/group (see User=/Group= in
# ../sip2nostr.service). Applied automatically by ../../install.sh;
# manual install: copy to /usr/local/lib/sysusers.d/ and apply with
# `systemd-sysusers`.

#Type  Name        ID  GECOS                Home directory             Shell
u      sip2nostr   -   "sip2nostr service"   /var/local/lib/sip2nostr   /usr/sbin/nologin
