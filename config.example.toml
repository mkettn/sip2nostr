[sip]
provider_host = "sip.your-provider.de"
username = "YOUR_SIP_USER"
password = "YOUR_SIP_PASS"
# Optional: set this to the public host/IP your SIP provider should use
# for inbound calls if REGISTER succeeds but no INVITE reaches this process.
# contact_host = "203.0.113.10"
# Local RTP port for SIP audio. Twinkle commonly uses 8000.
rtp_port = 8000

[dns]
# Address of the resolver to use for SIP hostname lookups.
# Falls back to system resolver if omitted.
resolver = "1.1.1.1:53"
resolver_fallback = "9.9.9.9:53"
timeout_ms = 2000

[logging]
# Optional: write each process run to its own log file.
# Relative paths are resolved next to this config file. If the path does
# not include {timestamp} or {run}, a timestamp is added before the extension.
run_file = "logs/sip2nostr-{timestamp}.log"
# Minimum level to log, case-insensitive: "verbose", "debug",
# "information", "warning", "error", or "fatal". Defaults to "warning" -
# turn this down to "information" or "debug" when troubleshooting.
level = "warning"

[[lines]]
uri = "sip:+4989123456@sip.your-provider.de"
label = "main"
# Optional: when [nostr].enabled is false, answer calls on this line and
# play this file on loop to test SIP audio. Raw 8 kHz 16-bit PCM works
# directly; mono Opus (.opus) is decoded in-process. See
# docs/sound-files.md for exactly what's supported and how to convert a
# file if needed.
# sound = "sounds/test.opus"

[[lines]]
uri = "sip:+4989123457@sip.your-provider.de"
label = "fax"

[nostr]
enabled = true
relays = ["wss://relay.example.com", "wss://relay2.example.com"]
bridge_nsec = "nsec1..."      # this daemon's own identity, added as a contact in the receiving client
target_npub = "npub1..."      # your identity - every call latches here in the MVP (see README)

[webrtc]
stun_servers = ["stun:stun.l.google.com:19302"]
turn_server = ""               # optional, recommended for NAT traversal
connection_loss_grace_seconds = 15  # how long a bridged call's WebRTC connection can sit "disconnected" before it's ended

[callerlist]
# A blacklist match always rejects the call, regardless of whitelist.
# An empty whitelist means blacklist-only mode: everyone not blacklisted
# is allowed through. A non-empty whitelist switches to deny-by-default:
# only whitelisted numbers get through (still overridable by a blacklist
# match). Numbers are normalized before comparison (spaces/dashes
# stripped, "00" international prefix folded to "+"), so either format
# works here.
blacklist = []
whitelist = []

[voicemail]
# Answering-machine fallback for when [nostr] is enabled but target_npub
# never answers the call. Opt-in - disabled unless set to true. See
# docs/voicemail.md.
enabled = false
# How long to wait for a WebRTC answer over Nostr before falling back.
ring_timeout_seconds = 20
# Caller is disconnected after this long even if they keep talking.
# Neither delivery mode below inlines the recording in the DM itself
# ("text" sends a transcript, "audio" sends a Blossom upload's URL), so
# this isn't a size budget - just a sanity limit on how much PCM is
# buffered in memory while recording. Raise it only as far as this host's
# available RAM comfortably allows for the busiest realistic case (one
# recording per line, worst case all lines at once). ConfigLoader rejects
# anything above 3600 (1 hour) at startup, so this can't itself become
# unbounded. See docs/voicemail.md. Replaces the old, now-removed
# max_text_recording_seconds - if your config still has that key, delete
# it and use this one as the ceiling for every delivery mode instead.
max_recording_seconds = 600
# Passed to Concentus when encoding a recording. Only takes effect if
# the writer's input and output sample rates ever differ -
# they don't today (both are the recording's own rate), so this
# currently has no observable effect; kept configurable for when that
# changes. 0 (fastest) - 10 (best); ConfigLoader rejects anything
# outside that range at startup.
opus_resampler_quality = 5
# Optional. Same format rules as [[lines]].sound: raw 8 kHz 16-bit PCM
# works directly, mono Opus (.opus) is decoded in-process. Falls back to
# a short tone if unset.
# greeting_sound = "sounds/greeting.opus"
# Where recordings are saved (as Opus), relative to this config file
# unless rooted. Always written, regardless of whether the Nostr send succeeds.
recordings_dir = "voicemail"
# Filename for a saved recording, relative to recordings_dir above.
# Placeholders: {timestamp} (yyyyMMdd-HHmmss), {caller} (the normalized
# caller number), {call_id} (a per-call unique id, not a phone number -
# useful if you'd rather not have the caller's number in filenames).
# Must include {timestamp} or {call_id} so recordings from different
# calls can't overwrite each other, must be a relative path (joined onto
# recordings_dir above - a rooted value would silently discard it), and
# must not contain a ".." segment - ConfigLoader rejects a value
# violating any of these at startup. A path separator is fine otherwise,
# e.g. "{caller}/{timestamp}.opus" to group recordings per caller.
recording_filename = "{timestamp}-{caller}.opus"
# Optional. Relays to publish the voicemail DM to, if different from
# [nostr].relays above - e.g. target_npub advertises a separate NIP-17
# DM inbox relay list (kind:10050). Falls back to the Nostr SDK's default
# NIP-17 relay resolution against [nostr].relays if left empty.
# dm_relays = ["wss://dm-relay.example.com"]
# "file" (default) - no upload, no transcription: the recording is just
# saved to recordings_dir above and a plain-text notice is sent instead,
# the zero-setup option. "audio" AES-GCM encrypts the recording, uploads
# only the ciphertext to a Blossom server (see [voicemail.blossom]
# below), and sends a NIP-17 file message with the URL and decryption
# key - requires at least one entry there. "text" transcribes the
# recording and sends the transcript instead (see
# [voicemail.transcription] below) - requires model_path there. If
# "audio"/"text"'s own requirement isn't configured, sip2nostr logs a
# startup warning and falls back to "file"'s behavior instead of failing
# to start - it does not fall back to inlining the recording in the DM.
# See docs/voicemail.md.
delivery = "file"

# Only consulted when [voicemail].delivery = "text".
[voicemail.transcription]
# The only engine available today.
engine = "whisper"
# Path to a GGML model file, e.g. one downloaded via whisper.cpp's
# models/download-ggml-model.sh script. Relative to this config file
# unless rooted. Leaving this unset when delivery = "text" isn't an
# error - see the [voicemail].delivery comment above.
# model_path = "models/ggml-base.en.bin"
# Optional. An ISO 639-1 code (e.g. "en"). Unset auto-detects the spoken
# language per recording, at some accuracy/latency cost.
# language = "en"

# Consulted when [voicemail].delivery = "audio", and also when delivery
# = "text" above and transcription produces nothing - in that case a
# Blossom upload is tried before falling back further to a plain-text
# notice. See docs/voicemail.md.
[voicemail.blossom]
# Blossom (BUD-01/BUD-02) server base URLs, tried in order until one
# accepts the upload. Authenticates every upload as [nostr].bridge_nsec's
# identity (a signed, time-limited kind 24242 event per BUD-02) - the
# server never sees the bridge's actual Nostr identity's private key, and
# never sees the recording's plaintext, only the AES-GCM ciphertext.
# Leaving this empty when delivery = "audio" isn't an error - see the
# [voicemail].delivery comment above.
# servers = ["https://blossom.example.com"]
